#include "main.h"
/**
 * _puts - prints a string, followed by new line to sdout
 * @str: string value
 * Return: no value
 */
void _puts(char *str)
{
	while (*str != '\0')
	{
		_putchar(*str);
		str++;
	}
	_putchar('\n');
}
